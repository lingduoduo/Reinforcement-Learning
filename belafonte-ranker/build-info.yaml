# the doc:
# https://backstage.spotify.net/docs/tingle/tingle-templates/scio-pipeline/
#
version: 2

secrets:
  bucket:  belafonte

template:
  type: scioPipeline

notifications:
  - ref: refs/heads/master
    failure:
      slack:
        - $author
  - ref: refs/pull/.*/merge
    failure:
      slack:
        - $author
